---
phase: 04-ui-polish
plan: 01
type: execute
---

<objective>
Change KiddoBot's accent color from orange to pink across all components and fix the ChevronRight import bug.

Purpose: Establish KiddoBot's visual identity with its registered pink accent (matching WaterBot's uniform sky accent pattern) and fix a runtime error in the ProgramFinder details toggle.
Output: All 5 KiddoBot files updated with pink accent, ChevronRight imported, build passes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tool-rebuilds/03-02-SUMMARY.md

# Key files (all on VPS via SSH):
# /root/vanderdev-website/src/pages/KiddoBot.jsx — main page (~22 orange refs + missing ChevronRight import)
# /root/vanderdev-website/src/components/kiddobot/EligibilityCalculator.jsx — ~12 orange refs
# /root/vanderdev-website/src/components/kiddobot/IntakeForm.jsx — ~9 orange refs
# /root/vanderdev-website/src/components/kiddobot/CountyRRLookup.jsx — 2 orange refs (incl. default param)
# /root/vanderdev-website/src/components/kiddobot/ProgramCard.jsx — 1 orange ref
# /root/vanderdev-website/src/lib/bots/ChatMessage.jsx — shared, DO NOT MODIFY (already supports pink in COLOR_PALETTE)

**Tech stack available:** React + Vite + Tailwind CSS
**Established patterns:** ChatMessage.jsx `getMarkdownComponents(accentColor)` supports `pink` in COLOR_PALETTE
**Constraining decisions:**
- Production-first: All changes via `ssh vps` — dev repo is read-only
- Shared components: DO NOT modify ChatMessage.jsx (affects all 3 bots)
- Bot registry accent color: pink

**Color swap scope (orange → pink):**
KiddoBot.jsx: ~22 lines (MODES colors, linkColor props, accentColor props, Tailwind classes)
EligibilityCalculator.jsx: ~12 lines (icon circles, input focus, checkboxes, buttons, accentColor prop)
IntakeForm.jsx: ~9 lines (step indicators, input focus, tags, buttons)
CountyRRLookup.jsx: 2 lines (default param, input focus)
ProgramCard.jsx: 1 line (link color)

**DO NOT change amber/yellow in EligibilityCalculator.jsx** — those are semantic warning colors, not accent colors.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap orange accent to pink across all 5 KiddoBot files</name>
  <files>
    /root/vanderdev-website/src/pages/KiddoBot.jsx,
    /root/vanderdev-website/src/components/kiddobot/EligibilityCalculator.jsx,
    /root/vanderdev-website/src/components/kiddobot/IntakeForm.jsx,
    /root/vanderdev-website/src/components/kiddobot/CountyRRLookup.jsx,
    /root/vanderdev-website/src/components/kiddobot/ProgramCard.jsx
  </files>
  <action>
    Via SSH on VPS, perform the following changes:

    **KiddoBot.jsx:**
    1. Add `ChevronRight` to the lucide-react import on line 11-12 (fixes runtime error on ProgramFinder details toggle)
    2. Change all MODES color values to 'pink' (lines ~47, 54, 61, 68) — uniform accent like WaterBot's sky
    3. Replace all `orange-500` → `pink-500`, `orange-400` → `pink-400`, `orange-600` → `pink-600`, `orange-300` → `pink-300` in Tailwind classes
    4. Change `linkColor="orange"` → `linkColor="pink"` (ChatMessage props)
    5. Change `accentColor="orange"` → `accentColor="pink"` (CountyRRLookup props)

    **EligibilityCalculator.jsx:**
    1. Replace all `orange-500` → `pink-500`, `orange-600` → `pink-600` in Tailwind classes
    2. Change `accentColor="orange"` → `accentColor="pink"` (CountyRRLookup prop)
    3. DO NOT touch amber/yellow warning box classes (lines ~257-278) — those are semantic

    **IntakeForm.jsx:**
    1. Replace all `orange-500` → `pink-500`, `orange-400` → `pink-400`, `orange-600` → `pink-600`, `orange-300` → `pink-300`

    **CountyRRLookup.jsx:**
    1. Change default param from `accentColor = 'orange'` to `accentColor = 'pink'`
    2. Replace `orange-500` → `pink-500` in focus class

    **ProgramCard.jsx:**
    1. Replace `orange-500` → `pink-500`, `orange-400` → `pink-400`

    Use `sed -i` for bulk replacement on each file. After sed, verify no double-replacements occurred (e.g., `pink-pink`).
  </action>
  <verify>
    ssh vps "grep -rn 'orange' /root/vanderdev-website/src/pages/KiddoBot.jsx /root/vanderdev-website/src/components/kiddobot/" — should return ONLY amber/yellow warning lines from EligibilityCalculator.jsx, zero orange-accent lines
    ssh vps "grep -c 'pink' /root/vanderdev-website/src/pages/KiddoBot.jsx" — should be ~22+
    ssh vps "grep 'ChevronRight' /root/vanderdev-website/src/pages/KiddoBot.jsx" — should appear in import AND usage
  </verify>
  <done>Zero orange accent references remain (only amber warnings preserved). ChevronRight imported. All 5 files updated.</done>
</task>

<task type="auto">
  <name>Task 2: Build and verify pink theme compiles</name>
  <files>/root/vanderdev-website/</files>
  <action>
    Run production build on VPS:
    ssh vps "cd /root/vanderdev-website && npm run build"

    If build fails, inspect error output and fix. Common issues:
    - Typo in sed replacement (e.g., extra hyphen, missing quote)
    - ChevronRight not in the correct import line position

    After build succeeds, verify Tailwind purged correctly by checking the CSS output includes pink utility classes.
  </action>
  <verify>ssh vps "cd /root/vanderdev-website && npm run build 2>&1 | tail -5" — should show successful build with no errors</verify>
  <done>Build succeeds. No TypeScript/JSX errors. Pink accent classes present in output CSS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>KiddoBot pink accent theme across all components — color swap from orange to pink in ~42 locations across 5 files, plus ChevronRight import bug fix</what-built>
  <how-to-verify>
    1. Visit: https://vanderdev.net/kiddobot
    2. Check mode selection cards all show pink accent styling
    3. Start a chat — verify message bubbles, links, and source pills use pink accent
    4. Open EligibilityCalculator — verify input focus rings, buttons, and checkboxes are pink
    5. Open ProgramFinder — click "Find Your Local R&R Agency" details toggle (was broken before ChevronRight fix)
    6. Check IntakeForm step indicators are pink
    7. Confirm NO orange accent anywhere in the UI (amber warning boxes in calculator are OK)
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Plan 04-02 (Responsive Layout), or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -rn 'orange' src/pages/KiddoBot.jsx src/components/kiddobot/` returns only amber/warning lines
- [ ] `npm run build` succeeds on VPS
- [ ] ChevronRight import present and ProgramFinder details toggle works
- [ ] Human verified pink accent across all surfaces
</verification>

<success_criteria>

- All orange accent references replaced with pink across 5 files
- ChevronRight import bug fixed
- Amber/yellow semantic warning colors preserved in EligibilityCalculator
- Build passes on VPS
- Human-verified pink theme renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-polish/04-01-SUMMARY.md`
</output>
