---
phase: 04-ui-polish
plan: 02
type: execute
---

<objective>
Add mobile-first responsive layout to KiddoBot and perform side-by-side visual QA against WaterBot.

Purpose: KiddoBot currently has zero responsive breakpoints — no `sm:`, `md:`, `lg:` classes anywhere. Mobile users get a desktop-only layout. This plan adds responsive design and confirms visual parity with WaterBot.
Output: KiddoBot renders correctly on mobile (375px), tablet (768px), and desktop (1024px+). Phase 4 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-polish/04-01-SUMMARY.md

# Key files (all on VPS via SSH):
# /root/vanderdev-website/src/pages/KiddoBot.jsx — main page (mode grid, chat area, input)
# /root/vanderdev-website/src/components/kiddobot/EligibilityCalculator.jsx — multi-input form
# /root/vanderdev-website/src/components/kiddobot/IntakeForm.jsx — multi-step form
# /root/vanderdev-website/src/pages/WaterBot.jsx — reference for responsive patterns

**Tech stack available:** React + Vite + Tailwind CSS
**Established patterns:** WaterBot responsive approach (reference implementation)
**Constraining decisions:**
- Production-first: All changes via `ssh vps`
- Shared components: DO NOT modify ChatMessage.jsx
- Bot UI polish standard: max-w-[85%] for message bubbles, rounded-xl, px-5 py-4 for bot bubbles

**Responsive targets:**
- Mobile (375px): Single column, stacked mode cards, full-width input, touch-friendly tap targets (min 44px)
- Tablet (768px): 2-column mode grid, comfortable spacing
- Desktop (1024px+): Current layout (4-column mode grid, sidebar-friendly)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responsive breakpoints to KiddoBot.jsx</name>
  <files>/root/vanderdev-website/src/pages/KiddoBot.jsx</files>
  <action>
    Via SSH on VPS, add responsive Tailwind classes to KiddoBot.jsx:

    **Mode selection grid:**
    - Change the mode card grid from fixed columns to responsive: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3`
    - Mode cards should stack on mobile, 2-up on tablet, 4-up on desktop

    **Chat message area:**
    - Message bubble max-width: `max-w-[95%] sm:max-w-[85%]` (wider on mobile for readability)
    - Reduce padding on mobile if needed: `px-3 py-3 sm:px-5 sm:py-4` for bot messages

    **Input area:**
    - Input field: ensure full width on mobile with proper padding
    - Send button: ensure touch-friendly minimum size (min-h-[44px] min-w-[44px])

    **Suggested questions / empty state:**
    - Stack vertically on mobile, allow wrapping on tablet+: `flex flex-col sm:flex-row sm:flex-wrap gap-2`

    **Overall page container:**
    - Add horizontal padding for mobile: `px-3 sm:px-4 lg:px-6`
    - Ensure no horizontal overflow at 375px viewport

    **Reference WaterBot.jsx** for responsive patterns if available:
    ssh vps "grep -n 'sm:\|md:\|lg:' /root/vanderdev-website/src/pages/WaterBot.jsx | head -20"
    Use same breakpoint conventions for consistency across bots.
  </action>
  <verify>
    ssh vps "grep -c 'sm:\|md:\|lg:' /root/vanderdev-website/src/pages/KiddoBot.jsx" — should be 10+ responsive classes added
    ssh vps "cd /root/vanderdev-website && npm run build 2>&1 | tail -5" — build succeeds
  </verify>
  <done>KiddoBot.jsx has responsive breakpoints for mode grid, message area, input, and page container. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add responsive breakpoints to sub-components</name>
  <files>
    /root/vanderdev-website/src/components/kiddobot/EligibilityCalculator.jsx,
    /root/vanderdev-website/src/components/kiddobot/IntakeForm.jsx
  </files>
  <action>
    Via SSH on VPS, add responsive Tailwind classes:

    **EligibilityCalculator.jsx:**
    - Form inputs grid: responsive columns `grid grid-cols-1 sm:grid-cols-2 gap-4`
    - Result cards: responsive stacking
    - Buttons: full width on mobile `w-full sm:w-auto`
    - County R&R section: ensure dropdown doesn't overflow on mobile

    **IntakeForm.jsx:**
    - Step indicators: horizontal on desktop, compact on mobile (numbers smaller, connector thinner)
    - Form fields: full width stacking on mobile
    - Tag pills (selected items): `flex flex-wrap gap-2` with proper overflow handling
    - Navigation buttons: stack on mobile `flex flex-col sm:flex-row gap-2`

    DO NOT change CountyRRLookup.jsx or ProgramCard.jsx — they're simple enough to flow naturally.
  </action>
  <verify>
    ssh vps "grep -c 'sm:\|md:\|lg:' /root/vanderdev-website/src/components/kiddobot/EligibilityCalculator.jsx" — should be 5+
    ssh vps "grep -c 'sm:\|md:\|lg:' /root/vanderdev-website/src/components/kiddobot/IntakeForm.jsx" — should be 5+
    ssh vps "cd /root/vanderdev-website && npm run build 2>&1 | tail -5" — build succeeds
  </verify>
  <done>EligibilityCalculator and IntakeForm have responsive layouts. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full responsive layout for KiddoBot — mode grid, chat area, input, calculator form, and intake form all responsive from 375px to 1024px+</what-built>
  <how-to-verify>
    **Desktop (1024px+):**
    1. Visit: https://vanderdev.net/kiddobot
    2. Mode cards display in a 4-column grid
    3. Chat area has comfortable width, message bubbles at max-w-85%
    4. EligibilityCalculator form fields side-by-side

    **Tablet (768px) — use browser DevTools responsive mode:**
    5. Mode cards display in 2-column grid
    6. Chat area fills available width
    7. Calculator inputs stack or remain 2-col

    **Mobile (375px) — use browser DevTools responsive mode:**
    8. Mode cards stack in single column
    9. Message bubbles expand to 95% width for readability
    10. Input area full width with touch-friendly send button
    11. No horizontal scrollbar at any viewport size
    12. IntakeForm steps and inputs stack cleanly
    13. Suggested questions stack vertically

    **Side-by-side with WaterBot:**
    14. Open https://vanderdev.net/waterbot in adjacent tab
    15. Compare: similar visual weight, consistent spacing patterns, both bots feel like they belong to the same product
    16. KiddoBot should feel pink like WaterBot feels sky-blue

    **Cross-tool flows:**
    17. In chat, ask about eligibility → verify CTA links to calculator work
    18. In calculator, verify county R&R section renders correctly
    19. Confirm no console errors in browser DevTools
  </how-to-verify>
  <resume-signal>Type "approved" if Phase 4 is complete, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds on VPS
- [ ] Responsive classes present in KiddoBot.jsx, EligibilityCalculator.jsx, IntakeForm.jsx
- [ ] No horizontal overflow at 375px viewport
- [ ] Human verified desktop, tablet, and mobile layouts
- [ ] Side-by-side comparison with WaterBot approved
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- KiddoBot responsive at 375px, 768px, and 1024px+
- No horizontal overflow or layout breakage at any viewport
- Visual parity with WaterBot confirmed
- Phase 4: UI/UX Polish complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-polish/04-02-SUMMARY.md`

This is the final plan in Phase 4. Success criteria includes "Phase 4 complete."
</output>
